({
	invoiceClick: function(cmp, event, helper) {
        helper.invoice(cmp);
    },

    closeClick: function(cmp, event, helper) {
        helper.close(cmp);
    },

    doInit: function(cmp, event, helper) {
        helper.doInit(cmp);
    },
})